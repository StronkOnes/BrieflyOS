
import React from 'react';
import { Typography, Box } from '@mui/material';

function CRM() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" gutterBottom>
        CRM
      </Typography>
      <Typography variant="body1">
        This is the CRM page. Content to be added.
      </Typography>
    </Box>
  );
}

export default CRM;
