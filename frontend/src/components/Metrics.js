
import React from 'react';
import { Typography, Box } from '@mui/material';

function Metrics() {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" gutterBottom>
        Metrics
      </Typography>
      <Typography variant="body1">
        This is the Metrics page. Content to be added.
      </Typography>
    </Box>
  );
}

export default Metrics;
